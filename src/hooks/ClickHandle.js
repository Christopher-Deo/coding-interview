

const handleClick = () => {
    // insert code to be run
};

//button example with onClick event
<button
    type="button"
    className='--btn --btn-primary --btn-lg'
    onClick={() => generateActivity()}>
    Generate New Activity
</button>;